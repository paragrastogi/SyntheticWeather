# Declare default colours for the code which calls this script.
import numpy as np

# Deep blue.
blue = np.array((25, 100, 200)) / 255 
# Pure f***ing blue.
bluest = np.array((0, 0, 255)) / 255 

# Distinguished looking grey.
grey = np.array((0.3, 0.3, 0.3))
# Also distinguished but contrasts better with black.
lgrey = np.array((0.6, 0.6, 0.6)) 

# Indian flag orange (saffron).
orange = np.array((255, 128, 0)) / 255
# Orange like nice satsumas.
orangest = np.array((255, 165, 0)) / 255 

# Oxygenated blood red.
red = np.array((160, 30, 30)) / 255 
# Pure f***ing red.
reddest = np.array((255, 0, 0)) / 255 

# Gandalf white.
whitest = np.array((1, 1, 1))
# Black as coal, black as night.
blackest = np.array((0, 0, 0))

# Veering towards gold.
yellow = np.array((237, 177, 32)) / 255 
# Do not use unless you REALLY need to.
teal = np.array((50, 180, 165)) / 255 

# Gentle dark green.
green = np.array((40, 180, 20)) / 255
# Pure f***ing green.
greenest = np.array((0, 255, 0)) / 255 